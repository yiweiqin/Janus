.DS_Store
assets/.DS_Store
experiments/runs/
assets/.DS_Store
.local-data/
.local-runtime/
node_modules/
.venv*/
outputs/
scripts/__pycache__/
**/__pycache__/
*.pyc
experiments/rdmd_runs/
experiments/rdmd_detective_dataset/sft/*.jsonl
experiments/rdmd_detective_dataset/sft/smoke/
# data/ 下的语料与 sft/ 同性质：由 generate.mjs / prepare_sft.mjs 派生，动辄 200MB+。
# 只提交 manifest/labels/summary 这类小文件作为出处锚点，不提交语料本体。
experiments/rdmd_detective_dataset/data/*.jsonl
# 探针写出的派生图与逐图明细（`_graphs/` 下的 json）：可复跑重建，别进版本库。
# 真的需要钉住某次结果时，把它的 `generatedAt` 与关键计数抄进受追踪的 md，而不是提交 json。
experiments/rdmd_detective_dataset/ubuddy_recon/_graphs/
*.orig
*.bak
*.bak-*
# 本机 SSH 端点（含明文密码），换盒子时只改这一个文件。
scripts/_rdmd_env.ps1
.env
.env.local
.env.*.local
deploy/env/orgbench-init.env
config/codex/auth.json
*.pem
!*.pem.example
# 打包产物：`build-runtime/` 是复制进来的 Python 运行时（~99MB）与 trial-provider bundle，
# `test-artifacts/` 是 electron-builder 的输出与分阶段快照（~1GB）。两者都可重建，不进版本库。
/build-runtime/
/test-artifacts/
# sim_task_group 的派生件：\.bundle*\ 是 stage 出来的上传包（_sim_stage.mjs 可复跑重建）。
experiments/sim_task_group/.bundle/
experiments/sim_task_group/.bundle.tar.gz
# \out/\ 里的**大件**（generated.jsonl 4.7MB、briefs、sim_out.tar.gz）照样忽略 —— 可复跑重建。
experiments/sim_task_group/out/**
!experiments/sim_task_group/out/
experiments/sim_task_group/out/*
!experiments/sim_task_group/out/remote/
experiments/sim_task_group/out/remote/*
!experiments/sim_task_group/out/remote/out/
experiments/sim_task_group/out/remote/out/*
# **但报告本身是锚点，要进库。** 理由与 CPDB 的 BUNDLE.json 是同一条：
# `docs/ubuddy-v4-closed-loop-report.zh-CN.md` 里的每个数字都出自下面这几份，
# 而它们是**当时那一轮**的结果 —— 读的人得能自己核对，不能只信那页散文。
# 不钉的话，盒子一关、本机这份一丢，报告里的数字就再没有第二处可查。
# 注意：`.gitignore` 不允许"父目录被忽略、子文件再放行"，所以上面是一层层往前放行的。
!experiments/sim_task_group/out/remote/out/collect_report.json
!experiments/sim_task_group/out/remote/out/tpm_report.json
!experiments/sim_task_group/out/remote/out/jobs.jsonl
!experiments/sim_task_group/out/remote/out/verdicts.jsonl
!experiments/sim_task_group/out/remote/out/collect_rows.jsonl
# 八步闸门的逐行输出（worker 权重/源码 sha、migrationHead、协议版本检查都在这里）。
# 报告页第 1 节那张「步 → 验的是什么 → 结论」的表，出处就是它。
!experiments/sim_task_group/out/remote/run_remote.stdout.log

# ---------------------------------------------------------------------------
# cpdb_org_world：与 rdmd 语料同一条纪律 —— **派生语料不进库，锚点进库**
# ---------------------------------------------------------------------------
# 这个目录此前 62 个文件全部未跟踪（干净克隆会静默丢掉整份 CPDB 判分包）。
# 收进版本库时要分开看：谁是"载体"（代码、世界定义、真实标注），谁是"产物"。
#
# 1. `annotation_cards.jsonl`（11.6MB）与 `pairs.jsonl`（4.9MB）由
#    `generate.mjs` + `prelabel.mjs` 从 world/ 与 lib/ 派生，重跑即得。
#    把 16MB 的 jsonl 放进去，只会让每次 diff 都变成"整行重写"，而它证明不了任何东西 ——
#    能证明的是生成它的代码与种子。
experiments/cpdb_org_world/data/*/annotation_cards.jsonl
experiments/cpdb_org_world/data/*/pairs.jsonl
# 2. `export/ai-judge-v1/` 是 `export_bundle.mjs` 的输出（~20MB，且与上面两份重复），
#    它要**发出去**而不是存起来：它的用途是交给外部 AI/人判分，判完的结果再经
#    `import_ai_labels.mjs` 回来。所以要钉住的是 BUNDLE.json 里的 sha256 与行数
#    （那正是它存在的意义），不是那 20MB 本身。
experiments/cpdb_org_world/export/
# 3. `human_labels.jsonl` **不在此列、要进库**：它是人工标注，不是派生量，
#    丢了不可重建。同理 world/ 下的 orgs/people/agents/profiles 是世界的定义。
#
# 上面第 3 条类推到打分器这一层，就是下面这四类。判据只有一条：
# **这份东西是不是「跑一遍 `npm run` 就能原样回来」**。
# 4. `train_out*/` 里的 X.f32 / *.i8 / rows.jsonl / labels.jsonl / baselines.jsonl
#    全部由 `export_training_matrix.mjs` 从「世界数据 + 标签文件 + features.mjs」派生，
#    重跑即得；而 X.f32 是 3MB 二进制，diff 里什么都看不见。
#    **但 `matrix.json` 必须进库**：它是一页锚点（featureSpec、维度、特征名、词表、
#    划分计数、以及 inputs/outputs 的逐文件 sha256）。报告里的每个数都对得上它，
#    而它只有 5KB。这与第 2 条里 BUNDLE.json 的角色是同一个。
experiments/cpdb_org_world/train_out/*
!experiments/cpdb_org_world/train_out/matrix.json
experiments/cpdb_org_world/train_out_ai/*
!experiments/cpdb_org_world/train_out_ai/matrix.json
# 5. `data/full/ai_labels_qwen3_8b_v1.jsonl` **要进库**，尽管它带"AI"两个字。
#    它不是 `generate.mjs` 那种「seed 一给就能重放」的派生量：它是一次 GPU 判决的产物，
#    取决于模型、问法与采样种子。而 `artifacts/*/metrics.json` 的 `inputs` 里
#    逐字记着它的 sha256 —— 不把它放进来，那个哈希就指向空气，
#    随产物一起发出去的「这个模型是在哪批标签上训的」就无从核对。
#    体量（2.3MB）与同一目录下受追踪的 `human_labels.jsonl`（2.4MB）同级，不构成新问题。
# 6. `artifacts/cpdb-scorer-*/` **要进库**：`weights.json` 就是交付物本身；
#    `metrics.json` 是它的证据；`predictions.jsonl` 是 JS/Python 对账测试的基准
#    （`scorer.test.mjs` 逐条对齐它），缺了干净克隆上这条测试直接跑不起来。
#    三者合计约 5MB，且都是"当时那一次"的结果，不是中间量。
#    例外：`artifacts/_*` 是冒烟跑出来的草稿（`_smoke`），不是交付物，不进库。
experiments/cpdb_org_world/artifacts/_*/
# 7. `exports/cpdb-training-set-v1/` **不进库**，与第 2 条的 `export/ai-judge-v1/` 同类：
#    它是**发出去**的交付包（X.f32 3MB + folds 1.9MB + rows 0.9MB + labels 0.6MB），
#    里面每一份都是 `export_training_matrix.mjs` 从「世界 + 标签 + features」重跑即得的派生量。
#    要钉住的是 `artifacts/cpdb-scorer-v3-ship/manifest.json` 里那串逐文件 sha256 ——
#    那正是它存在的意义：证明"你复现的就是我交付的"。包本身可以随时重建：
#      `npm run experiment:cpdb-matrix` → `npm run experiment:cpdb-ship`
#    注意 `ship.test.mjs` 会在缺包时**跳过并说明怎么重建**，不会把"包不在"变成假失败。
experiments/cpdb_org_world/exports/
#
# 注：`.gitignore` 不允许「父目录被忽略、子文件再放行」，所以上面用的是逐个放行的写法。

# ---------------------------------------------------------------------------
# 本机/派生，不进库
# ---------------------------------------------------------------------------
# 生成的快照目录（里面是 zip 与一次性 build 产物，9MB）。归档靠 git 历史，不靠再存一份。
archive/
# catboost 训练过程的中间输出（learn_error.tsv / tfevents），由训练脚本重建。
catboost_info/
# 本机的 agent 记忆目录，不是仓库内容。
.workbuddy/
# 与上面 deploy/env/orgbench-init.env 同性质的本机环境文件。
.orgbench_env
# 训练/评测的一次性日志（`_v3_*.log`）：数字要留下来就抄进受追踪的 md，别提交日志。
scripts/_v3_*.log
